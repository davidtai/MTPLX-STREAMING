"""W21: DeepSeek-V4.1-Flash mxfp4 serve-profile + planner (CPU-only, no artifact load).

Skeleton committed first (session-restart safety). Real assertions follow.
"""

from __future__ import annotations


def test_skeleton_placeholder() -> None:
    # Replaced by the real profile/planner/selfcheck tests below.
    assert True
