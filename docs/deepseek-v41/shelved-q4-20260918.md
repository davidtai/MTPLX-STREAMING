# Q4 checkpoint, 2026-09-18

The user requested shelving Q4 optimization, saving all DeepSeek V4.1
workspaces, and opening a draft PR on `davidtai/MTPLX-STREAMING` before work
on the Q2 main model. No new GPU run accompanies this checkpoint.

The retained native MXFP4 target result is **13.4141517619 decode tokens/s**
on exactly 16,384 Python prompt tokens and 1,024 output IDs. The timed 1,023
decode steps take 76.2627423750 seconds. All native output IDs match, with
SHA-256 `0d54d9b28a180c2c91ff5ef14f0dfb38320014bbed9d01827fb1b60c6e0417ac`.
The sampled machine peak is 109,238,927,360 bytes against the 110,000,000,000
byte ceiling. This is a single full result, not a repeated isolated speedup.
See [the complete receipt](receipts/hybrid-lookup-20260918/README.md).

The branch contains the DeepSeek V4.1 port, streamed experts and Engram,
native DSpark MTP, memory accounting and admission corrections, bounded
runner experiments, and their receipts. Experimental benchmark helpers and
the attested strict allocator are explicit dependencies of the best result;
the result does not describe an unchanged production-service default.

## Preserved workspaces

[The inventory](checkpoints/20260918-worktrees.json) records 140 worktrees,
139 distinct original heads, and seven dirty snapshots. Each has a protected
`refs/checkpoints/deepseek-v41-20260918T161816Z/` ref. Snapshot commits preserve
tracked and nonignored untracked files without moving existing branches or
altering any original index or working file. W126 and W128 unfinished changes
remain separate from this PR's integration branch.

The local backup directory is:

```text
/Users/davidtai/projects/OpenSourceWTF/checkpoints/deepseek-v41-20260918T161816Z
```

It contains the inventory, staged/unstaged binary patches for dirty worktrees,
the strict allocator's three runtime files with SHA-256 checksums, and the
portable `all-workspaces.bundle`. Ignored model weights, virtual environments
and large benchmark tensors remain in their original locations. None of the
140 worktrees is removed or reset.

To inspect a saved dirty snapshot without altering the original worktree,
use its `checkpoint_commit` from the inventory with
`git worktree add --detach <new-directory> <checkpoint-commit>`. The bundle
preserves the checkpoint refs and the final Q4 PR head for recovery elsewhere.

## Verification and unfinished work

Fresh CPU-only checkpoint verification passes 37 memory-reporting regressions,
parses all 256 changed Python code files, verifies 38 new Q2-draft receipt
members, and checks both saved service-restoration receipts. Real MLX imports
are blocked during the focused pytest invocation. No broad suite or new
full-model benchmark is run for this shelving request.

**20 TPS and complete 256K prefill verification remain unfinished.** The Q2
experiments in this branch quantize the draft only: no fresh Q2 main-model
generation or output sanity check was performed. The next worktree must test
the Q2 target and inspect its generated Python output before accepting a
throughput claim. The older Q2 target failure is documented in
[W9_REPORT.md](W9_REPORT.md); it is evidence to compare with the new recipe.
