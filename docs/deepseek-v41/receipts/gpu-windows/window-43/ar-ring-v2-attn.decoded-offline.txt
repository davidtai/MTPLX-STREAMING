# offline decode of receipt token_ids (W106 in-run decode wrote empty text)
# stream: ar  n=257  eos_index=None
**Summary:** The module is a large, observability-heavy generation loop for MTPLX with extensive env-var gating, runtime counters, and decode tracing, but it currently lacks any aggregate summary helper over `GenerationOutput` sequences.

- The module already has strong per-request telemetry (`GenerationStats`, `_DecodeTrace`, `_runtime_counter_delta`) but no cross-request aggregation layer, so a `summarize_generation_outputs` helper would slot in naturally alongside `_generation_rate_fields` and reuse the existing `tok_s`/`decode_tok_s`/`end_to_end_tok_s` conventions.
- Optional metrics like prefill TPS, decode TPS, and peak memory are already conditionally populated (e.g., `paged_kv_capacity_tokens`, `mlx_memory` stats), so the helper should treat missing/zero fields as "unavailable" rather than coercing them to zero, matching the module's existing tolerant-delta style in `_DecodeTrace._delta`.
- The module's type style favors `Literal` aliases, `@dataclass` outputs, and `dict[str, Any]` returns with explicit `float`/`int` casts
