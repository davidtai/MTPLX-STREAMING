--- a/scripts/deepseek_v41/run_f2_prefetch_window.sh
+++ b/scripts/deepseek_v41/run_f2_prefetch_window.sh
@@ -170,6 +170,10 @@
 F16SITE="${F16SITE:-/Users/davidtai/projects/OpenSourceWTF/mtplx-hy3-ssd/.worktrees/dsv41-f16-pipeline/.f16-site}"   # private greenlet install
 F17DIR="${F17DIR:-/Users/davidtai/projects/OpenSourceWTF/mtplx-hy3-ssd/.worktrees/dsv41-f17-rows/scripts/deepseek_v41/f17}"
 F17_ORACLE_SHAPE="${F17_ORACLE_SHAPE:-81,54,59,14,32,25,0,9,18,10,3,15,29,28,0,30,0,9,0,54,0,0,0,15,0,0,0,27,0,6,13,28,27,34,10,7,21,32,55,55}"
+# F39 +tcq: run decode from the tcq3 (eschamoe K=3 trellis) expert bank. TCQPKG is the F39 package (parent of
+# tcq/ and trellis/); TCQ3_ART is the tcq3 artifact used as the candidate model dir (must carry expert-manifest.json).
+TCQPKG="${TCQPKG:-/Users/davidtai/projects/OpenSourceWTF/mtplx-hy3-ssd/.worktrees/dsv41-f39-tcq3-runtime/scripts/deepseek_v41}"
+TCQ3_ART="${TCQ3_ART:-/Users/davidtai/models/DeepSeek-V4.1-Flash-MTPLX-streaming-tcq3}"
 STAMP="$(date +%Y%m%d-%H%M%S)"
 STAGE_ROOT="/private/tmp/dsv41-f2b-${STAMP}"
 OUT_STAGE="/tmp/dsv41-110-stage"                     # run_full.py:330 requires --out here
@@ -195,7 +199,7 @@
 
 # --------------------------------------------------- 2. stage patched runner copies
 stage_tree() {  # $1 dest  $2 max_rows  $3 stage the F2b/GIL hook (0/1)  $4 stage F6 engram (0/1)  $5 charge the ring (0/1)
-  local dest="$1" rows="$2" f2b="$3" eng="${4:-0}" ring="${5:-0}" vc="${6:-0}" gt="${7:-0}" rd="${8:-0}" pl="${9:-0}" pipe="${10:-0}" ro="${11:-0}" ct="${12:-0}" cl="${13:-0}" prof="${14:-0}"
+  local dest="$1" rows="$2" f2b="$3" eng="${4:-0}" ring="${5:-0}" vc="${6:-0}" gt="${7:-0}" rd="${8:-0}" pl="${9:-0}" pipe="${10:-0}" ro="${11:-0}" ct="${12:-0}" cl="${13:-0}" prof="${14:-0}" tcq="${15:-0}"
   local ring_arg=""
   [ "$ring" != "0" ] && ring_arg="--ring-bytes $ring"   # total host bytes charged to admission
   if [ -e "$dest" ]; then echo "REFUSE: staged tree exists: $dest"; exit 2; fi
@@ -287,13 +291,20 @@
   fi
   if [ "$f2b" = "1" ] && [ "$pipe" != "1" ] && [ "${OTHER_PROMPT:-0}" != "1" ]; then
     nice -n 19 "$PYBIN" "$F2PKG/f2/stage_f2_runner.py" --run-full "$dest/packed/run_full.py"   # (other-prompt: applied above with --other-prompt)
+  fi
+  if [ "$tcq" = "1" ]; then   # F39: stage the tcq3 route LAST (edits the already-staged packed_phase.py + run_full.py) then drop the tcq/trellis packages so the child resolves `import tcq.*`
+    PYTHONPATH="$TCQPKG" nice -n 19 "$PYBIN" "$TCQPKG/tcq/stage_tcq_runner.py" \
+      --packed-phase "$dest/packed/packed_phase.py" --out-packed-phase "$dest/packed/packed_phase.py" \
+      --run-full "$dest/packed/run_full.py" --out-run-full "$dest/packed/run_full.py"
+    cp -R "$TCQPKG/tcq" "$dest/packed/tcq"
+    cp -R "$TCQPKG/trellis" "$dest/packed/trellis"
   fi
   PYTHONPATH="$F2PKG" nice -n 19 "$PYBIN" -m f2.window_preflight --no-seams \
     $(find "$dest" -maxdepth 2 -name '*.py' | sed 's/^/--compile /')
 }
 parse_arm() {  # $1 arm token -> A_BASE A_SI A_ENG A_ROWS A_F2B A_HOOK A_ENGSTAGE A_DIRNAME
   local tok="$1" mods
-  A_CAPS8=0; A_PIPE=0; A_BAL=0; A_BH=0; A_ST=0; A_OPS=0; A_MB=0; A_WM=0; A_RO=0; A_PF=0; A_LA=0; A_SY=0; A_REFGEN=0; A_CT=0; A_PW=0; A_CL=0; A_PROF=0; A_FI=0; A_FO=0
+  A_CAPS8=0; A_PIPE=0; A_BAL=0; A_BH=0; A_ST=0; A_OPS=0; A_MB=0; A_WM=0; A_RO=0; A_PF=0; A_LA=0; A_SY=0; A_REFGEN=0; A_CT=0; A_PW=0; A_CL=0; A_PROF=0; A_FI=0; A_FO=0; A_TCQ=0
   A_BASE="${tok%%+*}"; A_SI=0; A_ENG=0; A_VC=0; A_GT=0; A_PN=0; A_K0=0; A_FT=0; A_RIO=0; A_RD=0; A_CMP=0; A_PC=0; A_ML=0; A_PL=0; A_CMPSET=""
   mods="+${tok#*+}+"; [ "$tok" = "$A_BASE" ] && mods="+"
   case "$mods" in *"+si+"*) A_SI=1 ;; esac
@@ -361,6 +372,15 @@
       [ "$A_PIPE" = "1" ] || { echo "+fo only valid on a pipe base arm (got base '$A_BASE' in '$tok'): the F33 fast-observe lever needs the F16 two-group pipeline"; exit 2; }
       grep -q "MTPLX_DSV41_F33_FAST_OBSERVE" "$F16PKG/f16/install.py" 2>/dev/null || { echo "+fo needs an F16 package that reads MTPLX_DSV41_F33_FAST_OBSERVE (set F16PKG to <repo>/.worktrees/dsv41-f33-host-trims/scripts/deepseek_v41; got F16PKG=$F16PKG)"; exit 2; } ;;
   esac
+  # F39 +tcq: tcq3 (eschamoe K=3) decode lane (pipe base only; LOSSY by design). Sets the candidate model dir to
+  # TCQ3_ART, arms MTPLX_DSV41_TCQ3=1, and stages the tcq3 route from TCQPKG (loader + admission retarget +
+  # growth-transition + plane-lane). Whole-token match so it cannot collide with +ct/+ft.
+  case "$mods" in
+    *"+tcq+"*) A_TCQ=1
+      [ "$A_PIPE" = "1" ] || { echo "+tcq only valid on a pipe base arm (got base '$A_BASE' in '$tok'): the tcq3 decode lane rides the F16 two-group pipeline"; exit 2; }
+      [ -f "$TCQ3_ART/expert-manifest.json" ] || { echo "+tcq needs TCQ3_ART to point at a tcq3 artifact with expert-manifest.json (got TCQ3_ART=$TCQ3_ART)"; exit 2; }
+      grep -q "route_plane_lane" "$TCQPKG/tcq/install.py" 2>/dev/null || { echo "+tcq needs the F39 tcq package (set TCQPKG to <repo>/.worktrees/dsv41-f39-tcq3-runtime/scripts/deepseek_v41; got TCQPKG=$TCQPKG)"; exit 2; } ;;
+  esac
   A_HOOK=0; { [ "$A_F2B" = "1" ] || [ "$A_SI" = "1" ]; } && A_HOOK=1
   A_ENGSTAGE=0; [ "$A_ENG" != "0" ] && A_ENGSTAGE=1
   A_HOSTBYTES=0
@@ -370,14 +390,14 @@
   [ "$A_PL" != "0" ] && A_HOSTBYTES=$((A_HOSTBYTES + 100663296))   # F17 append-peak under-count (<= 67 MB), charged as 96 MiB
   A_ROSTAGE=0; { [ "$A_RO" != "0" ] || [ "$A_SY" != "0" ]; } && A_ROSTAGE=1
   local tree_suffix=""; [ "${OTHER_PROMPT:-0}" = "1" ] && tree_suffix="-op-$F2_PROMPT"   # F23 recipe key: other-prompt trees never mix with the benchmark's
-  A_TREE="$STAGE_ROOT/r${A_ROWS}-h${A_HOOK}-e${A_ENGSTAGE}-g${A_HOSTBYTES}-v${A_VC}-t${A_GT}-d${A_RD}-p${A_PL}-q${A_PIPE}-o${A_ROSTAGE}-c${A_CT}-l${A_CL}-f${A_PROF}${tree_suffix}"   # g = host ring charged to admission; c = F25 confidence threshold (0 = off); l = F27 cycle-log packed_phase hook; f = F28 host-profile packed_phase hook
+  A_TREE="$STAGE_ROOT/r${A_ROWS}-h${A_HOOK}-e${A_ENGSTAGE}-g${A_HOSTBYTES}-v${A_VC}-t${A_GT}-d${A_RD}-p${A_PL}-q${A_PIPE}-o${A_ROSTAGE}-c${A_CT}-l${A_CL}-f${A_PROF}-x${A_TCQ}${tree_suffix}"   # g = host ring charged to admission; c = F25 confidence threshold (0 = off); l = F27 cycle-log packed_phase hook; f = F28 host-profile packed_phase hook; x = F39 tcq3 route (0 = off)
   A_DIRNAME="$(printf '%s' "$tok" | tr '+' '_')"
 }
 ARMS="${F2_ARMS:-control_a candidate control_b}"
 echo "== stage retained sources -> $STAGE_ROOT (arms: $ARMS; probe=$F2_PROBE) =="
 for arm in $ARMS; do   # stage EVERY needed tree up-front: fail before the first unload
   parse_arm "$arm"
-  [ -d "$A_TREE" ] || stage_tree "$A_TREE" "$A_ROWS" "$A_HOOK" "$A_ENGSTAGE" "$A_HOSTBYTES" "$A_VC" "$A_GT" "$A_RD" "$A_PL" "$A_PIPE" "$A_ROSTAGE" "$A_CT" "$A_CL" "$A_PROF"
+  [ -d "$A_TREE" ] || stage_tree "$A_TREE" "$A_ROWS" "$A_HOOK" "$A_ENGSTAGE" "$A_HOSTBYTES" "$A_VC" "$A_GT" "$A_RD" "$A_PL" "$A_PIPE" "$A_ROSTAGE" "$A_CT" "$A_CL" "$A_PROF" "$A_TCQ"
 done
 if [ "${F2_STAGE_ONLY:-0}" = "1" ]; then   # CPU dry run of the whole staging sequence
   for arm in $ARMS; do parse_arm "$arm"; echo "STAGED $arm -> $A_TREE (rows=$A_ROWS f2b=$A_F2B si=$A_SI engram=$A_ENG verify_chunks=$A_VC conf=$A_CT policy_window=$A_PW flat_indices=$A_FI fast_observe=$A_FO cycle_log=$A_CL host_profile=$A_PROF growth=$A_GT native_predictor=$A_PN k0=$A_K0 first_target=$A_FT reader_io=$A_RIO row_dump=$A_RD compile=$A_CMP cpu_predictor=$A_PC wired_ring=$A_ML per_layer_rows=$A_PL compile_set=$A_CMPSET caps8=$A_CAPS8 host_bytes=$A_HOSTBYTES refgen=$A_REFGEN other_prompt=${OTHER_PROMPT} ar_mode=$([ "$A_REFGEN" = "1" ] && echo generate || echo reuse))"; done
@@ -403,7 +423,7 @@
 
 # ------------------------------------------------------------------ per-arm launcher
 run_arm() {  # $1 arm token (parse_arm must have run for it)
-  local arm="$A_DIRNAME" tree="$A_TREE" f2b="$A_F2B"
+  local arm="$A_DIRNAME" tree="$A_TREE" f2b="$A_F2B" MODEL_DIR="$MODEL_DIR"   # MODEL_DIR shadowed so +tcq can point it at the tcq3 artifact for this arm only
   local dir="$RECEIPTS/arm-${arm}"
   if [ -e "$dir" ]; then echo "REFUSE: $dir exists (never overwrite a measurement)"; exit 2; fi
   mkdir -p "$dir"
@@ -483,6 +503,11 @@
     pypath="$pypath:$F6DIR"
     f2b_env="$f2b_env MTPLX_DSV41_F6_ENGRAM_PARALLEL=1 MTPLX_DSV41_F6_INSTALL=$A_ENG MTPLX_DSV41_F6_ENGRAM_WORKERS=$F2_ENGRAM_WORKERS"
   fi
+  if [ "$A_TCQ" = "1" ]; then   # F39: tcq3 decode lane -- candidate model dir (--model + GPU_WINDOW_CANDIDATE_MODEL_DIR) + arm flag + package on PYTHONPATH
+    MODEL_DIR="$TCQ3_ART"
+    pypath="$pypath:$TCQPKG"
+    f2b_env="$f2b_env MTPLX_DSV41_TCQ3=1"
+  fi
   local f23_env=""
   if [ "$OTHER_PROMPT" = "1" ]; then   # F23: env pins the --other-prompt run_full reads at construction
     f23_env="DSV41_STAGE_PROMPT_IDS_FILE=$PROMPT_IDS DSV41_STAGE_PROMPT_IDS_SHA256=$DSV41_STAGE_PROMPT_IDS_SHA256 DSV41_STAGE_AR_REFERENCE_COMMIT=$DSV41_STAGE_AR_REFERENCE_COMMIT DSV41_STAGE_AR_MODE=$armode"
@@ -508,7 +533,7 @@
   echo "$rc" > "$dir/guard.exit"
   # copy the run_full receipt + sidecars (<stem>.*) into the arm dir.
   cp "${stem}".* "$dir/" 2>/dev/null || true
-  if [ "$A_PIPE" = "1" ] && [ "$A_CMP" != "1" ]; then   # the pipeline has an exact oracle: the sequential 4+rest digest
+  if [ "$A_PIPE" = "1" ] && [ "$A_CMP" != "1" ] && [ "$A_TCQ" != "1" ]; then   # the pipeline has an exact oracle: the sequential 4+rest digest
     local got; got="$(grep -a -o '"output_ids_sha256": "[0-9a-f]*"' "$dir"/f2b-*.passes.jsonl 2>/dev/null | head -1 | cut -d'"' -f4)"
     local want="$F16_ORACLE_SHA"; [ "$A_BAL" = "1" ] && want="${F16_BAL_ORACLE_SHA:-}"
     [ "$OTHER_PROMPT" = "1" ] && [ "$A_BAL" != "1" ] && want=""   # F23: no benchmark sequential oracle for a new prompt (see header)
@@ -519,6 +544,9 @@
       grep -a -E "ABORTED|Traceback|Error" "$dir/guard.log" | tail -5 || true; exit 4
     fi
     [ -n "$want" ] && echo "  pipe arm ${arm}: digest == its sequential row-split oracle (bit-identical arithmetic)"
+  elif [ "$A_PIPE" = "1" ] && [ "$A_TCQ" = "1" ]; then   # F39: tcq3 is LOSSY by design -- log the oracle mismatch and continue; never STOP the window on it, and skip the tie-classification (no meaningful tie vs a lossy bank)
+    local got; got="$(grep -a -o '"output_ids_sha256": "[0-9a-f]*"' "$dir"/f2b-*.passes.jsonl 2>/dev/null | head -1 | cut -d'"' -f4)"
+    echo "  tcq arm ${arm}: digest '${got:-none}' differs from the mxfp4 oracle by design (lossy tcq3 bank); NOT gated, continuing"
   fi
   if [ "$A_REFGEN" = "1" ]; then   # F23: refgen only emits the AR reference; DSpark may tie-flip vs AR (exit 1) or abort (exit 4). The receipt is written before the parity gate.
     if [ -s "$out" ]; then
@@ -590,6 +618,11 @@
 for arm, rows, digest, tps, wall, ctr, receipt in arms:
     if receipt is None:
         print(f"  {arm}: NO RECEIPT (guard.exit={_read(receipts/('arm-'+arm)/'guard.exit')})"); ok = False; continue
+    if "tcq" in arm:   # F39: tcq3 is lossy by design -- log the digest, never gate it against the mxfp4 oracle
+        rows_by_arm[arm] = rows
+        print(f"  {arm}: rows={rows} decode_tok_s={tps} decode_wall_s={wall} sha={digest} (tcq3 lossy -- not gated)"
+              + (f" f2b={ctr}" if ctr else ""))
+        continue
     rows_by_arm[arm] = rows
     match = (digest == ref_sha); ok = ok and match
     print(f"  {arm}: rows={rows} decode_tok_s={tps} decode_wall_s={wall} sha={'OK' if match else 'MISMATCH ' + str(digest)}"
